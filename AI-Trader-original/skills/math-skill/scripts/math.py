"""
Math operations for trading calculations.
"""


def add(a: float, b: float) -> float:
    """Add two numbers."""
    return float(a) + float(b)


def multiply(a: float, b: float) -> float:
    """Multiply two numbers."""
    return float(a) * float(b)
